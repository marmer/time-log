import DevModeView from "./DevModeView";

describe("DevModeView", () => {
    beforeEach(() => {
        localStorage.clear()
    });

    it("should show the google drive spike", async () => {

    });
});
